import TailwindConfig, {Theme} from "../../types/tailwindConfig";

function typography( configObj: TailwindConfig ){
  let result = [];
  const category = "Typography";
  configObj.theme

  
  
}

const plugins = []



export default function tailwind(){

}